# Default configuration

# TODO: add documentation to each of the configurations

name = "default"

[model]
name = "unet"
num_classes = 5

[train]
optimizer = 'adam'
loss = 'cross_entropy'
epsilon = 1e-8
alpha  = 0.25
gama = 2.0
lr = 0.001
lr_momentum = 0.7

